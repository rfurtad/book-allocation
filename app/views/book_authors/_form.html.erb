<%= form_with(model: book_author) do |form| %>
  <% if book_author.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(book_author.errors.count, "error") %> prohibited this book_author from being saved:</h2>

      <ul>
        <% book_author.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :book_id, style: "display: block" %>
    <%= form.text_field :book_id %>
  </div>

  <div>
    <%= form.label :author_id, style: "display: block" %>
    <%= form.text_field :author_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
